#include <stdio.h>

/**
  * add - adds two numbers
  *
  * @num1: first numeber
  * @num2: second number
  *
  * Return: Adds the two number an returns the number
*/

int add(int n, int m)
{
	return (n + m);
}
