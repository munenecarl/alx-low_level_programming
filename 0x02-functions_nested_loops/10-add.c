#include <stdio.h>

/**
  * add - adds two numbers
  *
  * @n: first numeber
  * @m: second number
  *
  * Return: Adds the two number an returns the number
*/

int add(int n, int m)
{
	return (n + m);
}
